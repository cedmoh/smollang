# Command Line Interface (CLI)

This directory contains the code for the command line interface.
The CLI is the main way to interact with the language.

## Features

Parsing the source code and printing the AST.

```
cargo run --bin cli -- parse path/to/your/file.smol
```

Add the `--no-color` flag to disable colors in the output.

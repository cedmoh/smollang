# Parser

This directory contains the parser. The parser is responsible for taking a string representation of a program,
converting it to `pest` rules, and then converting those rules into an abstract syntax tree (AST).
The AST is then used by the compiler to generate code.
